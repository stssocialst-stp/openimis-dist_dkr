#version: "3.7"
x-api: &default-api
  image: ghcr.io/stssocialst-stp/kenon-backend:latest
  build: 
    context: ./openimis-be_py/
    dockerfile: Dockerfile
  environment:
    - DB_DEFAULT=${DB_DEFAULT:-postgresql}
    - DB_HOST=${DB_HOST}
    - DB_PORT=${DB_PORT}
    - DB_NAME=${DB_NAME}
    - DB_USER=${DB_USER}
    - DB_PASSWORD=${DB_PASSWORD}
    - SITE_ROOT=api
    - SITE_URL=${DOMAIN}
    - CELERY_BROKER_URL=amqp://rabbitmq
    - MODE=${MODE:-Prod}
    - CSRF_TRUSTED_ORIGINS=${CSRF_TRUSTED_ORIGINS}
  volumes:
    - photos:/kenon-backend/openIMIS/images/insurees
  depends_on:
    db:
      condition: service_healthy
  networks:
    openimis-net:
  restart: always

services:
  frontend:
    container_name: ${PROJECT_NAME:-kenon}-frontend
    image: ghcr.io/stssocialst-stp/kenon-frontend:latest
    build: 
      context: ./openimis-fe_js/
      dockerfile: Dockerfile
    restart: always
    env_file: .env
    environment:
      - REACT_APP_API_URL=api
      - NEW_OPENIMIS_HOST=${DOMAIN}
      - OPENIMIS_CONF_JSON=${OPENIMIS_FE_CONF_JSON}
      - OPENSEARCH_BASIC_TOKEN=${OPENSEARCH_BASIC_TOKEN}
    networks:
      openimis-net:
    ports:
      - ${HTTP_PORT:-80}:80
      - ${HTTPS_PORT:-443}:443
    depends_on:
      - backend
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
      - ./conf/nginx:/conf/

  backend:
    <<: *default-api
    container_name: ${PROJECT_NAME:-kenon}-backend
    command: start
    env_file: .env
  
  worker:
    <<: *default-api
    container_name: ${PROJECT_NAME:-kenon}-worker
    command: worker
    env_file: .env

  rabbitmq:
    container_name: ${PROJECT_NAME:-kenon}-rabbitmq
    image: rabbitmq:3-management
    restart: always
    networks:
      openimis-net:
    ports:
      - 5672:5672

networks:
  openimis-net:
    name: "${PROJECT_NAME:-kenon}-net"

volumes:
  database:
  photos:
