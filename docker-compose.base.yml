x-api: &default-api
  image: ghcr.io/stssocialst-stp/kenon-backend:${BE_TAG:-latest}
  environment:
    - DB_DEFAULT=${DB_DEFAULT:-postgresql}
    - DB_HOST=${DB_HOST}
    - DB_PORT=${DB_PORT}
    - DB_NAME=${DB_NAME}
    - DB_USER=${DB_USER}
    - DB_PASSWORD=${DB_PASSWORD}
    - SITE_ROOT=api
    - SITE_URL=${DOMAIN}
    - CELERY_BROKER_URL=amqp://rabbitmq:5672
    - MODE=${MODE:-Prod}
    - CSRF_TRUSTED_ORIGINS=${CSRF_TRUSTED_ORIGINS}
    - FEED_DATA=${FEED_DATA:-False}
  volumes:
    - photos:/openimis-be/openIMIS/images/insurees
  networks:
    - kenon-stp-net
  restart: always

services:
  frontend:
    container_name: ${PROJECT_NAME:-openimis}-frontend
    image: ghcr.io/stssocialst-stp/kenon-frontend:${FE_TAG:-latest}
    restart: always
    env_file: .env
    environment:
      - REACT_APP_API_URL=api
      - NEW_OPENIMIS_HOST=${DOMAIN}
      - OPENIMIS_CONF_JSON=${OPENIMIS_FE_CONF_JSON}
      - OPENSEARCH_BASIC_TOKEN=${OPENSEARCH_BASIC_TOKEN}
    networks:
      - kenon-stp-net
    ports:
      - ${HTTP_PORT:-4005}:80
      - ${HTTPS_PORT:-4006}:443
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
      - ./conf/nginx:/conf/

  backend:
    <<: *default-api
    container_name: ${PROJECT_NAME:-openimis}-api
    command: start
    env_file: .env
    ports:
      - 4007:8000

  worker:
    <<: *default-api
    container_name: ${PROJECT_NAME:-openimis}-worker
    command: worker
    env_file: .env
    ports:
      - 4008:8000

  rabbitmq:
    container_name: ${PROJECT_NAME:-openimis}-rabbitmq
    image: rabbitmq:3-management
    restart: always
    networks:
      - kenon-stp-net
    ports:
      - 5672:5672
      - 4011:15672

networks:
  kenon-stp-net:
    external: true

volumes:
  database:
  photos:
